# Chat-only rehberler

Bu klasördeki dosyalar **sadece Chat RAG** için ingest edilir.

- Hedef: `knowledge_documents` / `knowledge_chunks`
- Yükleme: `python3 -m knowledge.ingest ...` veya `backend/ingest_chat_guides.py`

Bu içerikler **routine/analysis** tarafındaki `knowledge_base` tablosuna yüklenmez.

