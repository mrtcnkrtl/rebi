.env
deploy/.env.api
*.local

.DS_Store
**/.DS_Store

# Local PDF corpora (large / optional)
backend/DATA/

# Dependencies & env (never push)
node_modules/
backend/venv/
.venv/

# Large local backups / archives (GitHub 100MB limit)
backups/
*.tar.gz
